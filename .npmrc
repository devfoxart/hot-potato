# Configuration npm pour le projet Hot Potato

# Éviter les erreurs de peer dependencies
legacy-peer-deps=true

# Utiliser le registry npm officiel
registry=https://registry.npmjs.org/

# Timeout plus long pour les builds CI
timeout=300000

# Éviter les erreurs de cache
cache-min=0

# Configuration pour les builds automatisés
fund=false
audit=false